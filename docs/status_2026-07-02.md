# Score-Bundle Models — status, 2026-07-02

*Plain-language summary for review. The technical details, exact commands, and full
tables live in the repository (`docs/phase1_calibration_results.md`,
`docs/downstream_tasks_results.md`, `docs/formulation.tex`).*

**One-paragraph recap of the project.** We are building a probabilistic model that,
given the printed score and a recording of a performance (as piano MIDI for now),
recovers *how* each written note was actually played — its timing, its length
(articulation), and its loudness (dynamics) — together with an honest statement of how
confident the model is about each number. The key idea is a "score graph": notes that
are close in the score should be played in related ways, and the model uses that
structure both to fill in notes it cannot see well and to keep its confidence estimates
honest. A small music language model (a neural network we trained ourselves on the
MAESTRO piano corpus) supplies a first guess per note; the graph models what the guess
misses.

---

## Done

### 1. A single, clean mathematical write-up of the model (`docs/formulation.tex`)

The whole model — score, graph, prior, posterior, how hyperparameters are chosen, the
evaluation metrics, and the planned extensions to intonation/vibrato and audio — is now
stated once, in consistent notation matching the code. One notation clash was found and
fixed in the code's documentation (two symbols were being reused with different
meanings).

### 2. A correction to the headline result — found, fixed, and re-measured

While re-verifying the January pipeline we found a subtle flaw: the neural network's
per-note summary was computed at a position where it could *see the note's own loudness
value* — the very thing it was later asked to predict. Its apparently excellent loudness
predictions were partly it "copying its own input" rather than genuine prediction. We
first applied a conservative band-aid (recompute the summaries from the score alone,
every note fed a neutral placeholder loudness), then a principled fix: read the summary
*one position earlier* in the network, where it provably cannot have seen the note's own
loudness (we added an automated test that guarantees this), while still legitimately
using the surrounding notes. The principled fix is honest *and* recovers about a fifth
of the accuracy the band-aid had thrown away (loudness error 0.118 band-aid → 0.090
leak-free, vs the 0.057 the flawed pipeline claimed). It needs no retraining and is now
the default everywhere.

We also fixed a known numerical fragility: on a minority of evaluation runs the
automatic tuning of the model's noise level collapsed to zero, making the model
absurdly overconfident. A simple guard (the noise level may not drop below 5% of the
observed variability) fixes it. Both fixes were measured separately, one change at a
time.

**The corrected headline still supports the central claim, and now with statistical
significance.** On 30 held-out test pieces (never seen by the language model — we also
filter out any test performance whose twin appears in the language model's training
data) with 40% of notes hidden:

- The full model (language-model guess + graph) recovers hidden notes with the lowest
  error of all methods (RMSE 0.394, 95% CI [0.356, 0.417]) *and* the best-calibrated
  uncertainty (its "90% confidence" intervals contain the truth 92.2% of the time;
  log-score −0.314, the best of all methods).
- Paired comparisons across pieces show the graph's contribution is statistically
  significant on *both* accuracy and calibration (it beats the same guess without the
  graph on every axis), and the language-model guess adds a small but significant
  accuracy gain over using no guess at all.
- Honestly stated: the language model's advantage is *smaller* than the January numbers
  suggested — much of its apparent advantage on loudness was the flaw above.

### 3. Six downstream demonstrations that map where the graph structure helps

Chosen for having objective success measures and honest baselines. Five run on the ASAP
corpus; the sixth (performer identification) needed real performer labels, so we added the
small Vienna 4x22 corpus for it. Reporting all outcomes, positive and negative: three are
clear/qualified wins, two are mixed, and one is an honest negative — together they draw a
clean boundary around *where* the method adds value (see the summary at the end).

**(a) Error spotting (anomaly detection).** We deliberately corrupt 5% of notes (e.g. a
note played much too loud or held much too long) and ask each model to rank all notes
by suspiciousness. Ranking quality is measured by a standard score (AUROC; 0.5 = random
guessing, 1.0 = perfect). The graph model reaches **0.984** vs **0.943** for the
best structure-free baseline — and the gain holds on every one of timing, articulation,
and loudness. When the injected errors are made subtler (harder to spot), the graph's
advantage *grows* (0.85 → 0.94), because judging a note against its neighbours is exactly
what lets you notice one that is only slightly off for its local context. This task
rewards exactly what the thesis claims: trustworthy per-note uncertainty. **Verdict:
clear win.**

**(b) Cleaning up noisy transcriptions (denoising).** Automatic transcriptions of piano
recordings (the route to very large training corpora) are noisy. We simulate this by
adding noise to every note and asking models to recover the true values. When the noise
level is known, the graph model recovers the truth with clearly lower error than
per-note statistical shrinkage (the standard alternative) at equally honest confidence
(both ~90% coverage) — better on both axes, at every noise level tested. *Honest
negative:* estimating the noise level from a single piece is unreliable (the model
becomes overconfident); applications should calibrate the noise level per transcription
system, which is realistic, or estimate it across many pieces (future work).

**(c) Completing a performance from an excerpt (rendering).** Hear the performer play
only the opening (10–50% of the piece), predict the expression of everything after —
a much harder "extrapolation" task than the random-gaps benchmark. Here the two
ingredients separate cleanly: the language-model guess is what makes extrapolation work
(it generalizes across the whole piece where the hand-built baselines cannot), while the
graph structure helps only when the hidden notes have *nearby heard notes* to borrow
from — i.e. on filling scattered gaps, not on predicting a whole unheard second half.
**Verdict: partial** — use the language-model guess for the global shape and add the
graph where observed notes are close by.

**(d) Knowing when to trust a prediction (selective prediction).** If the model's
per-note confidence is meaningful, we should be able to set aside its least-confident
predictions and see the error on the rest drop. It does: for articulation, discarding the
least-confident half cuts the error by a third more than a model with a single
confidence-per-piece can manage; and in the one setting where the full model occasionally
misfires badly, its own uncertainty flags exactly those cases (error on the confident half
is a tenth of the overall error). **Verdict: yes, qualified** — the per-note confidence is
genuinely informative, most clearly for articulation and for catching its own mistakes.

**(e) Guessing the musical era from playing style alone (classification).** A harder,
more exploratory probe: can we tell Baroque from Classical from Romantic from Modern using
*only* how the piece is played (timing/articulation/dynamics statistics), never the notes
themselves? **Verdict: honest negative.** These features do not beat simply always
guessing the most common era, and cleaning them with the graph does not help. Bach's style
is distinguishable, but the later eras blur. The graph prior improves note-by-note
recovery and confidence — its designed purpose — not piece-level style summaries. A useful
boundary on where the method does and does not add value.

**(f) Identifying the pianist from how they play (Vienna 4x22 corpus).** The one task our
main dataset could not support — it has no performer labels. We added a second, small corpus
(the Vienna 4x22: 22 pianists each playing the *same* four excerpts, so the only difference
between recordings is the playing), which has real performer identities and, because everyone
plays identical notes, no confound between "style" and "the piece." We test whether we can
tell which of the 22 pianists is playing from the inferred expression alone, training on
three pieces and testing on the fourth (so the model must recognize a *style*, not memorize a
performance). **Two findings.** (1) *Yes, the expression carries the performer's identity* —
we reach 3–4× better than chance guessing, confirming that the timing/articulation/dynamics
we extract genuinely capture individual style, on real labels. (2) *Cleaning the expression
with the graph does not help here either* — it slightly hurts, for the same reason as the era
probe: smoothing removes the fine per-note detail that distinguishes players. **Verdict:
mixed** — a positive that validates our expression extraction, and a consistent negative that
the graph is a note-level tool, not a style-summary tool.

### 4. Two small, tested robustness upgrades (each measured one at a time)

- **Noise-level floor** (the fix above): turns the occasional overconfidence collapse
  into a non-event; on the headline it moves the full model's log-score from a spiky
  +0.08 (heavy-tailed across runs) to a clean −0.314, and makes the graph's advantage
  statistically significant.
- **Per-channel confidence rescaling** (fit on training pieces only): brings the "90%
  confidence" intervals from 92.2% down to 90.0% coverage exactly and lowers the
  calibration error, at a small log-score cost. Offered as an optional final step for
  when exact coverage is the requirement.

### 5. A finding that changes the scaling story

Re-running the "does a bigger language model give a better prior?" comparison under the
corrected (leak-free) setup: **the bigger model (1.3×, lower perplexity) gives an
essentially identical downstream result** (RMSE 0.394 vs 0.394). The apparent scaling
benefit in the January numbers was the same loudness leak — a lower-perplexity model
reconstructs its own loudness input more faithfully, which masqueraded as a better prior.
Practical implication: judge language-model quality by the *downstream* task, not
perplexity, before investing in scaling up on large (noisy, transcribed) corpora.

### 6. Do we need more data? (short answer: no — the limit is labels, not size)

- Five of the six tasks run on **ASAP**, the only corpus with aligned score and
  performance. It is sufficient, and no other dataset can substitute for the alignment.
- Larger MIDI collections (ATEPP, GiantMIDI, Aria-MIDI) would only help pre-train the
  language model *larger* — but finding 5 above shows that a bigger language model did not
  improve the result once the loudness flaw was removed, and those collections are
  machine-transcribed (noisier, and they risk overlapping our test pieces). So scaling the
  data up is currently low priority.
- What *did* need another dataset was **performer identification**, because it needs
  performer labels ASAP does not have. We added the small, purpose-built **Vienna 4x22**
  corpus (a quick, tiny download) and it is now wired in and run — task (f) above. The one
  remaining blocked task, spotting **real** (not injected) performance mistakes, needs a
  corpus with *annotated errors*, which does not yet exist in a usable form; raw volume
  would not unlock it.

### 7. Where the method helps, and where it does not (the boundary)

Across the six tasks a single, consistent line emerges: **the graph prior helps whenever the
score is scored note-by-note for accuracy or confidence, and does not help when expression is
collapsed into a whole-piece summary.**

- *Helps:* filling in hidden notes, spotting wrong notes, knowing which predictions to trust,
  cleaning known-level noise — all per-note tasks, all improved.
- *Does not help:* predicting a whole unheard second half from a short opening (the
  language-model guess carries that, not the graph), and the two classification tasks
  (musical era, performer) where we summarize a whole performance into a handful of numbers —
  there, smoothing with the graph removes useful detail.

This is exactly the thesis's own claim (structure + calibration *on the note field*), now
bounded by evidence on both sides rather than asserted.

## Work in progress

- All six downstream tasks are complete and written up.
- **Mask-aware summaries check (running now).** One residual honesty question about the
  leak-free fix: the network's input still contains the loudness of *other* hidden notes
  (never the note's own), and in principle information could flow back through the graph
  coupling. We are re-running the benchmark with a fully mask-aware variant (hidden
  notes' loudness replaced by the placeholder in the input, per evaluation run). If the
  numbers match, the leak-free fix is confirmed clean; if not, the mask-aware number
  becomes the honest headline. Either way the question closes with a measurement.

## To do (near term)

- Write the corrected results into the thesis draft; the January per-channel loudness
  claims must not be cited.
- ~~Feed the two fixes back into the default settings of all evaluation scripts.~~ Done —
  the leak-free read-out and the noise-level floor are now the defaults everywhere.
- Stage 2 of the language-model restart: a pretraining objective that matches how we
  read the network out (predict each note's expression from the score and *both*
  directions of context, with hidden-note masking built into training). Design sketched;
  gated on the mask-aware check above.

## To do (parked, unchanged)

- Intonation/vibrato (Phase 2) and audio likelihood (Phase 3).
- The geometric "connection Laplacian" direction (documented, deliberately not started
  this phase).
- Aria frozen-feature upper-bound baseline (aria not installed on this machine).
